package ch.tron.middleman.messagedto;

/**
 * Represents a message that is being sent from
 * {@link ch.tron.game} to {@link ch.tron.transport} and vise versa.
 */
public abstract class InAppMessage {
    // If a regular class extends an abstract class, then the class must
    // have to implement all the abstract methods of abstract parent class
    // or it has to be declared abstract as well.

    // =>
    // Do declare methods here only if EVERY sub class implements it.
}
